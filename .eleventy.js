module.exports = function(eleventyConfig) {

  // Add a filter using the Config API  
  // Do things.

  // Return the config object.
  return {
    dir: {
      input: "source",
      output: "build"
    }
  };
};