<!-- Navigation -->
<nav>
    <div>
        <a href="/">Tom<span>Anderson</span></a>
        <div>
        <ul>
            {%- for p in collections.all %}
            <li>
                <a{% if page.url == p.url %} class="active"{% endif %} href="{{ p.url }}">{{ p.data.title }}</a>
            </li>
            {%- endfor %}
        </ul>
        </div>
    </div>
</nav>